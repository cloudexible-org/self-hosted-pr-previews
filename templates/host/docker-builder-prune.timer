# /etc/systemd/system/docker-builder-prune.timer
# Enable with: systemctl enable --now docker-builder-prune.timer
[Unit]
Description=Cap Docker's build cache weekly

[Timer]
OnCalendar=weekly
Persistent=true

[Install]
WantedBy=timers.target
